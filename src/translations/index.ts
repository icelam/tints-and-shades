import appTranslations from './app';
import menusTranslations from './menus';
import dialogsTranslations from './dialogs';

const translations = {
  app: appTranslations,
  menus: menusTranslations,
  dialogs: dialogsTranslations
};

export default translations;
